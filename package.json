{
  "name": "neural-flow",
  "version": "1.0.0",
  "description": "Neural Flow - Executive Function Training",
  "type": "module",
  "main": "src/app.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:ui": "vitest --ui",
    "test:e2e": "playwright test",
    "test:coverage": "vitest run --coverage",
    "test:all": "vitest run && playwright test",
    "validate": "node scripts/validate.js && vitest run",
    "validate:full": "node scripts/validate.js && vitest run --coverage && playwright test"
  },
  "repository": {
    "type": "git",
    "url": "http://local_proxy@127.0.0.1:43845/git/LouisRosche/Neural-Flow"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.58.2",
    "@vitest/coverage-v8": "^4.1.0",
    "@vitest/ui": "^4.1.0",
    "jsdom": "^29.0.0",
    "vitest": "^4.1.0"
  }
}
